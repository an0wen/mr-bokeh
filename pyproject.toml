[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mrbokeh"
version = "1.0.1"

dependencies = [
    "numpy",
    "pandas",
    "bokeh",
]
[tool.setuptools]
py-modules = ['mrbokeh']